---
import { tagHref } from "../lib/paths";
import type { Lang } from "../lib/paths";

interface Props {
  lang: Lang;
  tag: string;
}

const { lang, tag } = Astro.props;
---

<a class="chip" href={tagHref(lang, tag)}>#{tag}</a>
